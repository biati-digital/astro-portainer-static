version: "3.7"
services:
  web:
    pull_policy: always
    #image: nginx:alpine
    deploy:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - 8080
    volumes:
      - website:/usr/share/nginx/html

volumes:
  website:
