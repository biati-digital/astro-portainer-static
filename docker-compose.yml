version: "3.7"
services:
  site:
    image: nginx:alpine
    build:
      context: .
      dockerfile: ./Dockerfile
    pull_policy: build
    ports:
      - 8080
    volumes:
      - website://usr/share/nginx/html

volumes:
  website:
