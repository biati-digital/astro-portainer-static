version: "3.7"
services:
  site:
    image: nginx:alpine
    build:
      context: .
      dockerfile: ./Dockerfile
    pull_policy: build
    ports:
      - 80
    volumes:
      - website:/public_html

volumes:
  website:
