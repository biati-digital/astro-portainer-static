version: "3.9"
services:
  web:
    build: ./
    pull_policy: build
    image: biatiastrojs
    restart: unless-stopped
    ports:
      - 80
      - 443
    volumes:
      - ./var/lib/docker/volumes/aruma_website:/usr/share/nginx/html
      #- website:/usr/share/nginx/html
