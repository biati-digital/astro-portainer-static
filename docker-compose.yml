version: "3.7"
services:
  site:
    image: node:lts-bullseye-slim
    pull_policy: always
    ports:
      - 8080
    volumes:
      - website:/usr/share/nginx/html
    command: >
      sh -c " npm install && npm run buld -- --mode custom "

volumes:
  website:
