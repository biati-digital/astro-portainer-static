version: "3.7"
services:
  site:
    pull_policy: always
    #image: nginx:alpine
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080
    volumes:
      - website:/usr/share/nginx/html

volumes:
  website:
