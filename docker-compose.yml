version: "3.9"
services:
  site:
    image: node:lts-bullseye-slim
    ports:
      - 8080:3000
    volumes:
      - site:/
    command: >
      sh -c "npm install && npm run build"

volumes:
  site:
